# Third-party assets

Everything in this repository is original work under the MIT licence in LICENSE,
with the exceptions and deliberate omissions recorded here.

## Shipped

| Asset | Origin | Terms |
|---|---|---|
| `site/assets/mark.svg` | Original. Three riders carried on one galloping horse, the line drawing traced to a single path. | MIT, with the rest of the repo |
| `site/assets/gallop-logo.png` | Original. The same drawing, filled. No longer used by the site; kept because the README history points at it. | MIT, with the rest of the repo |
| `site/assets/favicon.svg` | Original. A horseshoe, six nail holes, drawn as a ring sector for tab size; follows the OS colour scheme. | MIT |
| `site/assets/apple-touch-icon.png` | Original. The same horseshoe at 180px on a white tile. | MIT |
| `site/assets/gallop.css` | Original. The locked design system: two colours, three faces, a cell grid. | MIT |
| Archivo | Google Fonts, loaded by URL, not redistributed here | SIL Open Font License 1.1 |
| Courier Prime | Google Fonts, loaded by URL, not redistributed here | SIL Open Font License 1.1 |

## Deliberately not shipped

**Chicago font files.** The display role names Chicago, as the ChicagoFLF
revival. No binary is in this repository or in its history: the `@font-face`
rule resolves it with `local()` only, so a machine that already has Chicago uses
it and every other machine renders the display role in Archivo. That is what the
published site renders in today. Shipping Chicago means either clearing the
redistribution terms of a revival or drawing the display type as outlines;
`scripts/check_assets.py` refuses every font binary until that is settled.

**Macintosh icon set.** The design language no longer calls for one. Susan
Kare's 1984 Macintosh icons seeded the earlier Sys7 direction, and eight of them
were traced into the map and the intake as inline pixel runs. Those icons are
Apple's. They are gone from the working tree, but every commit from `045c9be`
to `7b5ab06` still carries them: git history is permanent, so removing them
there means rewriting it. The current system marks things with type and
hairlines rather than icons, so nothing replaces them.

The rule this repository follows: assets whose provenance is not clean never
enter the history, because git history is permanent and deleting a file later
does not remove it.
